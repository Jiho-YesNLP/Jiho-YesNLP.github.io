<!doctype html>
<html lang="{{ site.lang }}">
  <!-- Head -->
  <head>
    {% include head.liquid %}
  </head>

  <!-- Standalone page: no navbar, no footer, fills the viewport. -->
  <body class="cm-body">
    {{ content }}
  </body>
</html>
